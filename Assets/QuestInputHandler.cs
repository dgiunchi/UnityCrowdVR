using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestInputHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Debug.Log("From Quest Controller Primary:" + OVRInput.Get(OVRInput.Axis2D.PrimaryThumbstick).x);
        //Debug.Log("From Quest Controller Secondary:" + OVRInput.Get(OVRInput.Axis2D.SecondaryThumbstick).x);
    }
}
